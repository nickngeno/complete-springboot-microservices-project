server:
  port: 9095

spring:
  application:
    name: CONFIG-SERVER

  cloud:
    config:
      server:
        git:
          uri: https://github.com/nickngeno/microservice-config-server
          clone-on-start: true
          default-label: main